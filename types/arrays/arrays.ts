// Syntax
const names: string[] = []
names.push("John")

type Attendees = {
    names: string[]
    total: number
}

// Alternative syntax
const surnames: Array<string> = []
surnames.push("Doe")

// Multidimensional
const multiNumbers: number[][] = [
    [1, 2, 3, 4],
    [12, 53, 13, 2]
]